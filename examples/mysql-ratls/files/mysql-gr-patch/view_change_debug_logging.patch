diff --git a/plugin/group_replication/src/gcs_view_modification_notifier.cc b/plugin/group_replication/src/gcs_view_modification_notifier.cc
--- a/plugin/group_replication/src/gcs_view_modification_notifier.cc
+++ b/plugin/group_replication/src/gcs_view_modification_notifier.cc
@@ -22,6 +22,8 @@
 
 #include "plugin/group_replication/include/gcs_view_modification_notifier.h"
 
+#include <errno.h>
 #include <time.h>
 
 #include "my_dbug.h"
@@ -29,6 +31,24 @@
 #include "mysql/psi/mysql_cond.h"
 #include "plugin/group_replication/include/plugin_psi.h"
 
+/* Debug helper: get monotonic timestamp in seconds */
+static double get_monotonic_time() {
+  struct timespec ts;
+  clock_gettime(CLOCK_MONOTONIC, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get realtime timestamp in seconds */
+static double get_realtime() {
+  struct timespec ts;
+  clock_gettime(CLOCK_REALTIME, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get thread ID */
+#include <pthread.h>
+#define GET_THREAD_ID() ((unsigned long)pthread_self())
+
 Plugin_gcs_view_modification_notifier::Plugin_gcs_view_modification_notifier()
     : view_changing(false), cancelled_view_change(false), error(0) {
   mysql_cond_init(key_GR_COND_view_modification_wait, &wait_for_view_cond);
@@ -44,18 +64,36 @@ Plugin_gcs_view_modification_notifier::
 void Plugin_gcs_view_modification_notifier::start_view_modification() {
   mysql_mutex_lock(&wait_for_view_mutex);
   view_changing = true;
   cancelled_view_change = false;
   error = 0;
+  fprintf(stderr, "[VIEW-NOTIFIER] start_view_modification: notifier=%p thread=%lu "
+          "realtime=%.6f monotonic=%.6f\n",
+          (void*)this, GET_THREAD_ID(), get_realtime(), get_monotonic_time());
+  fflush(stderr);
   mysql_mutex_unlock(&wait_for_view_mutex);
 }
 
 bool Plugin_gcs_view_modification_notifier::is_view_modification_ongoing() {
   mysql_mutex_lock(&wait_for_view_mutex);
   bool result = view_changing;
   mysql_mutex_unlock(&wait_for_view_mutex);
   return result;
 }
 
 void Plugin_gcs_view_modification_notifier::end_view_modification() {
   mysql_mutex_lock(&wait_for_view_mutex);
+  fprintf(stderr, "[VIEW-NOTIFIER] end_view_modification: notifier=%p thread=%lu "
+          "view_changing=%d cancelled=%d realtime=%.6f monotonic=%.6f\n",
+          (void*)this, GET_THREAD_ID(), view_changing, cancelled_view_change,
+          get_realtime(), get_monotonic_time());
+  fflush(stderr);
   view_changing = false;
   mysql_cond_broadcast(&wait_for_view_cond);
   mysql_mutex_unlock(&wait_for_view_mutex);
@@ -64,6 +102,11 @@ void Plugin_gcs_view_modification_notifier::end_view_modification() {
 void Plugin_gcs_view_modification_notifier::cancel_view_modification(
     int errnr) {
   mysql_mutex_lock(&wait_for_view_mutex);
+  fprintf(stderr, "[VIEW-NOTIFIER] cancel_view_modification: notifier=%p thread=%lu "
+          "errnr=%d view_changing=%d realtime=%.6f monotonic=%.6f\n",
+          (void*)this, GET_THREAD_ID(), errnr, view_changing,
+          get_realtime(), get_monotonic_time());
+  fflush(stderr);
   view_changing = false;
   cancelled_view_change = true;
   this->error = errnr;
@@ -80,19 +123,52 @@ bool Plugin_gcs_view_modification_notifier::wait_for_view_modification(
     long timeout) {
   int result = 0;
 
+  double wait_start_realtime = get_realtime();
+  double wait_start_monotonic = get_monotonic_time();
+  fprintf(stderr, "[VIEW-NOTIFIER] wait_for_view_modification BEGIN: notifier=%p thread=%lu "
+          "timeout=%ld realtime=%.6f monotonic=%.6f\n",
+          (void*)this, GET_THREAD_ID(), timeout, wait_start_realtime, wait_start_monotonic);
+  fflush(stderr);
+
   mysql_mutex_lock(&wait_for_view_mutex);
 
   DBUG_EXECUTE_IF("group_replication_skip_wait_for_view_modification",
                   { view_changing = false; };);
 
+  int loop_count = 0;
   while (view_changing && !cancelled_view_change) {
     struct timespec ts;
     set_timespec(&ts, timeout);
+    double deadline_sec = (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+    double current_realtime = get_realtime();
+    double current_monotonic = get_monotonic_time();
+    fprintf(stderr, "[VIEW-NOTIFIER] wait_for_view_modification LOOP[%d]: notifier=%p "
+            "view_changing=%d cancelled=%d deadline=%.6f current_realtime=%.6f "
+            "current_monotonic=%.6f time_until_deadline=%.6f\n",
+            loop_count, (void*)this, view_changing, cancelled_view_change,
+            deadline_sec, current_realtime, current_monotonic,
+            deadline_sec - current_realtime);
+    fflush(stderr);
+
     result =
         mysql_cond_timedwait(&wait_for_view_cond, &wait_for_view_mutex, &ts);
 
-    if (result != 0)  // It means that it broke by timeout or an error.
-    {
+    double after_wait_realtime = get_realtime();
+    double after_wait_monotonic = get_monotonic_time();
+    int is_etimedout = (result == ETIMEDOUT);
+    fprintf(stderr, "[VIEW-NOTIFIER] wait_for_view_modification AFTER_WAIT[%d]: notifier=%p "
+            "result=%d is_ETIMEDOUT=%d errno=%d view_changing=%d cancelled=%d "
+            "realtime=%.6f monotonic=%.6f elapsed_realtime=%.6f elapsed_monotonic=%.6f\n",
+            loop_count, (void*)this, result, is_etimedout, errno,
+            view_changing, cancelled_view_change,
+            after_wait_realtime, after_wait_monotonic,
+            after_wait_realtime - wait_start_realtime,
+            after_wait_monotonic - wait_start_monotonic);
+    fflush(stderr);
+
+    if (result != 0) {
       view_changing = false;
       break;
     }
+    loop_count++;
   }
 
   DBUG_EXECUTE_IF("group_replication_force_view_modification_timeout",
@@ -101,6 +177,15 @@ bool Plugin_gcs_view_modification_notifier::wait_for_view_modification(
 
   mysql_mutex_unlock(&wait_for_view_mutex);
 
+  double wait_end_realtime = get_realtime();
+  double wait_end_monotonic = get_monotonic_time();
+  fprintf(stderr, "[VIEW-NOTIFIER] wait_for_view_modification END: notifier=%p thread=%lu "
+          "result=%d cancelled=%d error=%d total_elapsed_realtime=%.6f "
+          "total_elapsed_monotonic=%.6f realtime=%.6f monotonic=%.6f\n",
+          (void*)this, GET_THREAD_ID(), result, cancelled_view_change, error,
+          wait_end_realtime - wait_start_realtime,
+          wait_end_monotonic - wait_start_monotonic,
+          wait_end_realtime, wait_end_monotonic);
+  fflush(stderr);
+
   return (result != 0 || cancelled_view_change);
 }
 
diff --git a/plugin/group_replication/src/gcs_operations.cc b/plugin/group_replication/src/gcs_operations.cc
--- a/plugin/group_replication/src/gcs_operations.cc
+++ b/plugin/group_replication/src/gcs_operations.cc
@@ -31,6 +31,24 @@
 #include "plugin/group_replication/include/plugin_handlers/primary_election_invocation_handler.h"
 #include "plugin/group_replication/include/plugin_psi.h"
 
+/* Debug helper: get monotonic timestamp in seconds */
+static double gcs_ops_get_monotonic_time() {
+  struct timespec ts;
+  clock_gettime(CLOCK_MONOTONIC, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get realtime timestamp in seconds */
+static double gcs_ops_get_realtime() {
+  struct timespec ts;
+  clock_gettime(CLOCK_REALTIME, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get thread ID */
+#include <pthread.h>
+#define GCS_OPS_GET_THREAD_ID() ((unsigned long)pthread_self())
+
 Gcs_operations::Gcs_operations()
     : gcs_interface(nullptr),
       leave_coordination_leaving(false),
@@ -320,8 +338,14 @@ end:
 
 void Gcs_operations::notify_of_view_change_end() {
   view_observers_lock->rdlock();
+  fprintf(stderr, "[GCS-OPS] notify_of_view_change_end: thread=%lu list_size=%zu "
+          "realtime=%.6f monotonic=%.6f\n",
+          GCS_OPS_GET_THREAD_ID(), view_change_notifier_list.size(),
+          gcs_ops_get_realtime(), gcs_ops_get_monotonic_time());
   for (Plugin_gcs_view_modification_notifier *view_notifier :
        view_change_notifier_list) {
+    fprintf(stderr, "[GCS-OPS] notify_of_view_change_end: calling end_view_modification on notifier=%p\n",
+            (void*)view_notifier);
     view_notifier->end_view_modification();
   }
   view_observers_lock->unlock();
@@ -329,8 +353,14 @@ void Gcs_operations::notify_of_view_change_end() {
 
 void Gcs_operations::notify_of_view_change_cancellation(int error) {
   view_observers_lock->rdlock();
+  fprintf(stderr, "[GCS-OPS] notify_of_view_change_cancellation: thread=%lu error=%d list_size=%zu "
+          "realtime=%.6f monotonic=%.6f\n",
+          GCS_OPS_GET_THREAD_ID(), error, view_change_notifier_list.size(),
+          gcs_ops_get_realtime(), gcs_ops_get_monotonic_time());
   for (Plugin_gcs_view_modification_notifier *view_notifier :
        view_change_notifier_list) {
+    fprintf(stderr, "[GCS-OPS] notify_of_view_change_cancellation: calling cancel_view_modification on notifier=%p error=%d\n",
+            (void*)view_notifier, error);
     view_notifier->cancel_view_modification(error);
   }
   view_observers_lock->unlock();
@@ -345,9 +375,17 @@ bool Gcs_operations::is_injected_view_modification() {
 void Gcs_operations::remove_view_notifer(
     Plugin_gcs_view_modification_notifier *view_notifier) {
   if (nullptr == view_notifier) return;
 
   view_observers_lock->wrlock();
+  size_t size_before = view_change_notifier_list.size();
   view_change_notifier_list.remove(view_notifier);
+  size_t size_after = view_change_notifier_list.size();
+  fprintf(stderr, "[GCS-OPS] remove_view_notifer: notifier=%p thread=%lu "
+          "size_before=%zu size_after=%zu realtime=%.6f monotonic=%.6f\n",
+          (void*)view_notifier, GCS_OPS_GET_THREAD_ID(),
+          size_before, size_after,
+          gcs_ops_get_realtime(), gcs_ops_get_monotonic_time());
+  fflush(stderr);
   view_observers_lock->unlock();
 }
 
diff --git a/plugin/group_replication/src/gcs_event_handlers.cc b/plugin/group_replication/src/gcs_event_handlers.cc
--- a/plugin/group_replication/src/gcs_event_handlers.cc
+++ b/plugin/group_replication/src/gcs_event_handlers.cc
@@ -49,6 +49,24 @@
 #include "plugin/group_replication/include/services/notification/notification.h"
 #include "plugin/group_replication/include/udf/udf_utils.h"
 
+/* Debug helper: get monotonic timestamp in seconds */
+static double evt_get_monotonic_time() {
+  struct timespec ts;
+  clock_gettime(CLOCK_MONOTONIC, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get realtime timestamp in seconds */
+static double evt_get_realtime() {
+  struct timespec ts;
+  clock_gettime(CLOCK_REALTIME, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get thread ID */
+#include <pthread.h>
+#define EVT_GET_THREAD_ID() ((unsigned long)pthread_self())
+
 using std::string;
 using std::vector;
 
@@ -560,6 +578,12 @@ void Plugin_gcs_events_handler::on_view_changed(
   bool is_joining = is_member_on_vector(new_view.get_joined_members(),
                                         local_member_info->get_gcs_member_id());
 
+  fprintf(stderr, "[GCS-EVENT] on_view_changed BEGIN: thread=%lu is_joining=%d is_leaving=%d "
+          "joined_count=%zu leaving_count=%zu members_count=%zu view_id=%s "
+          "realtime=%.6f monotonic=%.6f\n",
+          EVT_GET_THREAD_ID(), is_joining, is_leaving,
+          new_view.get_joined_members().size(), new_view.get_leaving_members().size(),
+          new_view.get_members().size(), new_view.get_view_id().get_representation().c_str(),
+          evt_get_realtime(), evt_get_monotonic_time());
+  fflush(stderr);
+
   /*
     Maybe on_suspicions we already executed the above block but it was too
     late. No point in repeating the message, but we need to break the view
@@ -648,6 +672,10 @@ void Plugin_gcs_events_handler::on_view_changed(
       local_member_info->get_recovery_status() ==
           Group_member_info::MEMBER_ERROR &&
       !autorejoin_module->is_autorejoin_ongoing()) {
+    fprintf(stderr, "[GCS-EVENT] on_view_changed: MEMBER_ERROR detected, calling notify_of_view_change_cancellation "
+            "thread=%lu realtime=%.6f monotonic=%.6f\n",
+            EVT_GET_THREAD_ID(), evt_get_realtime(), evt_get_monotonic_time());
+    fflush(stderr);
     LogPluginErr(ERROR_LEVEL, ER_GRP_RPL_MEMBER_EXIT_PLUGIN_ERROR);
     gcs_module->notify_of_view_change_cancellation(
         GROUP_REPLICATION_CONFIGURATION_ERROR);
@@ -680,6 +708,10 @@ void Plugin_gcs_events_handler::on_view_changed(
     // update the Group Manager with all the received states
     if (update_group_info_manager(new_view, exchanged_data, is_joining,
                                   is_leaving) &&
         is_joining) {
+      fprintf(stderr, "[GCS-EVENT] on_view_changed: update_group_info_manager FAILED, calling notify_of_view_change_cancellation "
+              "thread=%lu realtime=%.6f monotonic=%.6f\n",
+              EVT_GET_THREAD_ID(), evt_get_realtime(), evt_get_monotonic_time());
+      fflush(stderr);
       gcs_module->notify_of_view_change_cancellation();
       return;
     }
@@ -714,6 +746,10 @@ void Plugin_gcs_events_handler::on_view_changed(
 
    // Signal that the injected view was delivered
     if (gcs_module->is_injected_view_modification()) {
+      fprintf(stderr, "[GCS-EVENT] on_view_changed: injected view, calling notify_of_view_change_end "
+              "thread=%lu realtime=%.6f monotonic=%.6f\n",
+              EVT_GET_THREAD_ID(), evt_get_realtime(), evt_get_monotonic_time());
+      fflush(stderr);
       gcs_module->notify_of_view_change_end();
+    }
 
     group_events_observation_manager->after_view_change(
@@ -859,6 +895,12 @@ void Plugin_gcs_events_handler::handle_joining_members(const Gcs_view &new_view,
   size_t number_of_joining_members = new_view.get_joined_members().size();
   size_t number_of_leaving_members = new_view.get_leaving_members().size();
 
+  fprintf(stderr, "[GCS-EVENT] handle_joining_members BEGIN: thread=%lu is_joining=%d is_leaving=%d "
+          "number_of_members=%zu number_of_joining=%zu number_of_leaving=%zu "
+          "realtime=%.6f monotonic=%.6f\n",
+          EVT_GET_THREAD_ID(), is_joining, is_leaving,
+          number_of_members, number_of_joining_members, number_of_leaving_members,
+          evt_get_realtime(), evt_get_monotonic_time());
+  fflush(stderr);
+
   /*
    If we are joining, 3 scenarios exist:
    1) We are incompatible with the group so we leave
@@ -867,7 +909,16 @@ void Plugin_gcs_events_handler::handle_joining_members(const Gcs_view &new_view,
   */
   if (is_joining) {
     int error = 0;
+    fprintf(stderr, "[GCS-EVENT] handle_joining_members: is_joining=true, calling check_group_compatibility "
+            "thread=%lu realtime=%.6f monotonic=%.6f\n",
+            EVT_GET_THREAD_ID(), evt_get_realtime(), evt_get_monotonic_time());
+    fflush(stderr);
     if ((error = check_group_compatibility(number_of_members))) {
+      fprintf(stderr, "[GCS-EVENT] handle_joining_members: check_group_compatibility FAILED error=%d, "
+              "calling notify_of_view_change_cancellation thread=%lu "
+              "realtime=%.6f monotonic=%.6f\n",
+              error, EVT_GET_THREAD_ID(), evt_get_realtime(), evt_get_monotonic_time());
+      fflush(stderr);
       gcs_module->notify_of_view_change_cancellation(error);
       return;
     }
@@ -895,6 +946,10 @@ void Plugin_gcs_events_handler::handle_joining_members(const Gcs_view &new_view,
       Only declare the view delivery complete after the above asserts,
       this will allow check them while join and automatic rejoin are
       still ongoing.
     */
+    fprintf(stderr, "[GCS-EVENT] handle_joining_members: calling notify_of_view_change_end "
+            "thread=%lu realtime=%.6f monotonic=%.6f\n",
+            EVT_GET_THREAD_ID(), evt_get_realtime(), evt_get_monotonic_time());
+    fflush(stderr);
     gcs_module->notify_of_view_change_end();
 
     update_member_status(
diff --git a/plugin/group_replication/src/plugin.cc b/plugin/group_replication/src/plugin.cc
--- a/plugin/group_replication/src/plugin.cc
+++ b/plugin/group_replication/src/plugin.cc
@@ -52,6 +52,24 @@
 #include "plugin/group_replication/include/udf/udf_registration.h"
 #include "plugin/group_replication/include/udf/udf_utils.h"
 
+/* Debug helper: get monotonic timestamp in seconds */
+static double plugin_get_monotonic_time() {
+  struct timespec ts;
+  clock_gettime(CLOCK_MONOTONIC, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get realtime timestamp in seconds */
+static double plugin_get_realtime() {
+  struct timespec ts;
+  clock_gettime(CLOCK_REALTIME, &ts);
+  return (double)ts.tv_sec + (double)ts.tv_nsec / 1000000000.0;
+}
+
+/* Debug helper: get thread ID */
+#include <pthread.h>
+#define PLUGIN_GET_THREAD_ID() ((unsigned long)pthread_self())
+
 using std::string;
 
 /* Plugin generic fields */
@@ -803,9 +821,22 @@ int initialize_plugin_and_join(
     goto err;
   }
 
+  fprintf(stderr, "[GR-PLUGIN] initialize_plugin_and_join: calling wait_for_view_modification "
+          "notifier=%p thread=%lu realtime=%.6f monotonic=%.6f\n",
+          (void*)view_change_notifier, PLUGIN_GET_THREAD_ID(),
+          plugin_get_realtime(), plugin_get_monotonic_time());
+  fflush(stderr);
+
   if (view_change_notifier->wait_for_view_modification()) {
+    fprintf(stderr, "[GR-PLUGIN] initialize_plugin_and_join: wait_for_view_modification RETURNED TRUE (timeout/error) "
+            "notifier=%p is_cancelled=%d thread=%lu realtime=%.6f monotonic=%.6f\n",
+            (void*)view_change_notifier, view_change_notifier->is_cancelled(),
+            PLUGIN_GET_THREAD_ID(), plugin_get_realtime(), plugin_get_monotonic_time());
+    fflush(stderr);
     if (!view_change_notifier->is_cancelled()) {
       // Only log a error when a view modification was not cancelled.
+      fprintf(stderr, "[GR-PLUGIN] initialize_plugin_and_join: TIMEOUT (not cancelled), logging error "
+              "thread=%lu realtime=%.6f monotonic=%.6f\n",
+              PLUGIN_GET_THREAD_ID(), plugin_get_realtime(), plugin_get_monotonic_time());
+      fflush(stderr);
       LogPluginErr(ERROR_LEVEL, ER_GRP_RPL_TIMEOUT_ON_VIEW_AFTER_JOINING_GRP);
     }
     error = view_change_notifier->get_error();
@@ -813,6 +844,12 @@ int initialize_plugin_and_join(
     goto err;
   }
   gcs_module->remove_view_notifer(view_change_notifier);
+  fprintf(stderr, "[GR-PLUGIN] initialize_plugin_and_join: wait_for_view_modification SUCCESS "
+          "notifier=%p thread=%lu realtime=%.6f monotonic=%.6f\n",
+          (void*)view_change_notifier, PLUGIN_GET_THREAD_ID(),
+          plugin_get_realtime(), plugin_get_monotonic_time());
+  fflush(stderr);
 
   transaction_consistency_manager->register_transaction_observer();
   transaction_consistency_manager->plugin_started();
