--- a/plugin/group_replication/libmysqlgcs/src/bindings/xcom/xcom/xcom_detector.h
+++ b/plugin/group_replication/libmysqlgcs/src/bindings/xcom/xcom/xcom_detector.h
@@ -27,7 +27,8 @@
 
 #include "xcom/site_struct.h"
 
-#define DETECTOR_LIVE_TIMEOUT 5.0
+/* Increased from 5.0 to 30.0 for RA-TLS in SGX environments */
+#define DETECTOR_LIVE_TIMEOUT 30.0
 
 #define DETECT(site, i) \
       (site->detected[i] + DETECTOR_LIVE_TIMEOUT > task_now())
